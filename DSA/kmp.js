function kmp(str, pattern) {
    let count = 0;
    for (let i = 0; i < pattern.length; i++){
        if (str[i] !== pattern[i]) {
            return count;;
        }
    }

    for (let j = k; j < str.length; j++){
        if(str)
    }
}